name: Lint and Test

on:
  pull_request:
    branches: ["dev", "main"]
  workflow_dispatch: {}

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
      - run: npm install
      - run: npm run lint
      - run: npm run test
